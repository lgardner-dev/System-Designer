SYSTEM DESIGNER — CONTROL-FLOW-FIRST WORKED STUDY

We are reviewing a native, read-only S2 inquiry example, not operating a workflow runner or adopting Foundry Method changes.

Design locally: establish outcome/constraints; describe the control flow; identify independently meaningful responsibilities; refine their information exchanges; reconcile behavior with available interfaces; then descend breadth-first as appropriate. The approximate eight-component guideline is advisory. A comparison, branch or local statement need not become an architectural component. Stop at a specified computation/predicate, bounded operation, or accountable semantic judgment whose inputs, actor and outcomes are explicit at this abstraction level. Do not disguise semantic judgments as mechanically evaluated predicates.

One authoritative component tree supports separate Control Flow and Interfaces views. A control transition is not a data connection; one transition can refer to several typed exchanges. Component definitions and action occurrences have separate identities. Never duplicate a component's authoritative interface merely because it is used more than once. Child boundaries reuse the owner's exact physical port IDs. No direct edge may bypass levels.

The provided `control-flow.study.json` uses the EXPERIMENTAL format `system-designer-control-flow-study`, version 1. This is not a version-1 replacement packet for the production application. It contains a bound original S2 source, two flow definitions, source mappings and the single proposed call/return boundary. Its renderer uses authored fixture positions; positions are not engineering truth. `interfaces.project.json` is an ordinary production `system-designer-project`, version 1, and can be opened in the current System Designer. Its exact existing contract catalog remains a draft inherited from the Atlas migration. Do not silently change those contract definitions.

The root candidate replaces the contiguous S2.hyp -> S2.plan -> S2.run region with the proposed component S2.experiment. The child still shows every original step. Expanding the call reconstructs all ten original steps and eleven transitions, including all conditions and contract associations. The `source` object is immutable comparison evidence, not a second editable design. Preserve chronology: seal the experiment before execution; distinguish observations from interpretation; obtain accountable disposition; retain both the prior-art bypass and truthful stop. A renewed inquiry requires the recorded revised-question/renewed-bound condition; never turn it into an unconditional retry.

Do not infer permission, currentness, successful execution or approval from a path, highlight, motion, or metric. M=4 is the locally modeled sequential CFG complexity, calculated with outcomes connected to one virtual analysis sink; it is not four possible executions or four tests proving correctness. The experiment child has M=1. Collapsing a view cannot reduce a metric, and source branch detail may be coarser than a future implementation.

This worked candidate is for review. Discuss proposed changes and identify exact IDs. Do not propose a production migration, automatically execute work, silently erase unshown interfaces, or claim that the study format is already adopted. The read-only application can copy the study and export the interface project; it does not apply returned changes. A future editable format and scoped behavior replacement protocol require a separately reviewed integration design. No AI account or runtime service is needed.
